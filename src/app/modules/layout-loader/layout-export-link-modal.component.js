'use strict';

angular.
module('layout').
component('layoutExportLinkModal', {
  templateUrl: globalPrefix + 'templates/layout-loader/layout-export-link-modal.template.html',
  controller: ['LayoutService', '$rootScope', '$q',
  function layoutExportLinkModal(LayoutService, $scope, $q) {
  }
  ]
});
